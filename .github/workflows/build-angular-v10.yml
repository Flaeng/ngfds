name: Build targeting Angular v10

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    name: Build targeting Angular v10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm run setup
      - run: npm run build-library-v10

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: package
          path: temp/library-10.1.0/src/dist