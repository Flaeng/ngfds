name: Build targeting Angular v12

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    name: Build targeting Angular v12
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm run setup
      - run: npm run build-library-v12

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: package
          path: temp/library-v12/src/dist