name: Build targeting Angular v14

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    name: Build targeting Angular 14
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm run setup
      - run: npm run build-library-v14

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: package
          path: temp/library-14.0.0/src/dist